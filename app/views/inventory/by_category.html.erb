<h1>WE ARE FURNITURE</h1>
<p>See all of our products in our Category below!</p>

<table>
	<tr>
		<th>Name</th>
		<th>Price</th>
		<th>Category</th>
	</tr>
	<% @products.each do |product| %>
	<tr>
		<td><%= link_to product.name, one_product_path(id: product.id) %></td>
		<td><%= number_to_currency product.price %></td>
		<td><%= link_to product.category, by_category_path(category: product.category) %></td>
	</tr>
	<% end %>
</table>
<br>
<br>
<br>
<%= link_to "Back to All Products", root_path %>